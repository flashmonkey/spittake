<%= form_for(@user_agent) do |f| %>
  <% if @user_agent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_agent.errors.count, "error") %> prohibited this user_agent from being saved:</h2>

      <ul>
      <% @user_agent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
