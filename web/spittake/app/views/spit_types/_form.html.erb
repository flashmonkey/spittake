<%= form_for(@spit_type) do |f| %>
  <% if @spit_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spit_type.errors.count, "error") %> prohibited this spit_type from being saved:</h2>

      <ul>
      <% @spit_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
