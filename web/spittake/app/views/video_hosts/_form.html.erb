<%= form_for(@video_host) do |f| %>
  <% if @video_host.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@video_host.errors.count, "error") %> prohibited this video_host from being saved:</h2>

      <ul>
      <% @video_host.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
